 for(int i=0;i<str.length();i++)
         {
             for(int j=i;j<str.length()-1;j++)
             {
                 if()
                 temp = a[i];
                 a[i]=a[j];
                 a[j]=temp;
             }
         }